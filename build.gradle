// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'checkstyle'

repositories {
    mavenCentral()
}


tasks.withType(Checkstyle) {
 ignoreFailures = true
 reports {
 html.enabled = true
 }
}

